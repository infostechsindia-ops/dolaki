# AuraMart Rider App Security & Session Architecture (RIDER-001)

## 1. Security Overview

The Rider App Architecture enforces biometric authentication (`Face ID / Touch ID / Fingerprint`), secure JWT token persistence, offline-safe read caching, and strict mutation blocking when disconnected.

# AuraMart Commerce OS — Redirect & URL Manager Guide

## Overview
Manages 301 (Permanent) and 302 (Temporary) HTTP redirects, custom vanity slugs, and broken link handling.

---

## 1. Capabilities
- Map legacy store paths (e.g. `/old-about` -> `/company/about`).
- Monitor 404 access attempts for broken URL remediation.
- Zero-downtime redirect rule propagation.
